public abstract class ArenaDrawer extends LanternaGUI {
    /*private int draw() throws IOException {
        screen.clear();
        if (arena.draw(screen.newTextGraphics()) == 1)
            return 1;
        screen.refresh();
        return 0;
    }*/
}
